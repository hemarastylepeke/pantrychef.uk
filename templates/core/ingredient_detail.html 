{% extends 'core/base.html' %}

{% block title %}{{ ingredient.name }} - Pantry Pilot{% endblock %}

{% block content %}
<div class="min-h-screen bg-gradient-to-br from-green-50 to-blue-50 py-8">
    <div class="container mx-auto px-4 max-w-4xl">
        <!-- Header -->
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8">
            <div>
                <h1 class="text-3xl md:text-4xl font-bold text-gray-800">{{ ingredient.name }}</h1>
                <div class="flex items-center gap-4 mt-2">
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium 
                        {% if ingredient.category == 'vegetables' %}bg-green-100 text-green-800
                        {% elif ingredient.category == 'fruits' %}bg-yellow-100 text-yellow-800
                        {% elif ingredient.category == 'dairy' %}bg-blue-100 text-blue-800
                        {% elif ingredient.category == 'meat' %}bg-red-100 text-red-800
                        {% else %}bg-gray-100 text-gray-800{% endif %}">
                        {{ ingredient.get_category_display }}
                    </span>
                    {% if ingredient.barcode %}
                    <span class="text-gray-600 text-sm">
                        Barcode: {{ ingredient.barcode }}
                    </span>
                    {% endif %}
                </div>
            </div>
            <div class="flex gap-2 mt-4 md:mt-0">
                <a href="{% url 'edit_ingredient' ingredient.id %}" 
                   class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-2 rounded-lg transition-colors duration-200 font-medium">
                    Edit
                </a>
                <a href="{% url 'delete_ingredient' ingredient.id %}" 
                   class="bg-red-500 hover:bg-red-600 text-white px-6 py-2 rounded-lg transition-colors duration-200 font-medium">
                    Delete
                </a>
                <a href="{% url 'ingredient_list' %}" 
                   class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-2 rounded-lg transition-colors duration-200 font-medium">
                    Back to List
                </a>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Left Column - Basic Info -->
            <div class="lg:col-span-2 space-y-6">
                <!-- Storage Instructions -->
                {% if ingredient.storage_instructions %}
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4">Storage Instructions</h3>
                    <p class="text-gray-600 leading-relaxed">{{ ingredient.storage_instructions }}</p>
                </div>
                {% endif %}

                <!-- Nutritional Information -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <h3 class="text-xl font-semibold text-gray-800 mb-6">Nutritional Information (per 100g)</h3>
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                        <div class="text-center p-4 bg-green-50 rounded-lg">
                            <div class="text-2xl font-bold text-green-600">{{ ingredient.calories }}</div>
                            <div class="text-sm text-gray-600">Calories</div>
                        </div>
                        <div class="text-center p-4 bg-blue-50 rounded-lg">
                            <div class="text-2xl font-bold text-blue-600">{{ ingredient.protein }}g</div>
                            <div class="text-sm text-gray-600">Protein</div>
                        </div>
                        <div class="text-center p-4 bg-yellow-50 rounded-lg">
                            <div class="text-2xl font-bold text-yellow-600">{{ ingredient.carbs }}g</div>
                            <div class="text-sm text-gray-600">Carbs</div>
                        </div>
                        <div class="text-center p-4 bg-red-50 rounded-lg">
                            <div class="text-2xl font-bold text-red-600">{{ ingredient.fat }}g</div>
                            <div class="text-sm text-gray-600">Fat</div>
                        </div>
                    </div>
                    {% if ingredient.fiber %}
                    <div class="mt-4 text-center p-4 bg-purple-50 rounded-lg max-w-xs mx-auto">
                        <div class="text-xl font-bold text-purple-600">{{ ingredient.fiber }}g</div>
                        <div class="text-sm text-gray-600">Fiber</div>
                    </div>
                    {% endif %}
                </div>

                <!-- Pantry Items -->
                {% if pantry_items %}
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4">Pantry Items with this Ingredient</h3>
                    <div class="space-y-3">
                        {% for item in pantry_items %}
                        <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                            <div>
                                <span class="font-medium text-gray-800">{{ item.user.username }}</span>
                                <span class="text-gray-600 text-sm ml-2">{{ item.quantity }} {{ item.unit }}</span>
                            </div>
                            <span class="text-sm {% if item.status == 'active' %}text-green-600{% else %}text-gray-500{% endif %}">
                                {{ item.get_status_display }}
                            </span>
                        </div>
                        {% endfor %}
                    </div>
                </div>
                {% endif %}
            </div>

            <!-- Right Column - Additional Info -->
            <div class="space-y-6">
                <!-- Quick Info -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4">Quick Info</h3>
                    <div class="space-y-3">
                        <div class="flex justify-between">
                            <span class="text-gray-600">Category:</span>
                            <span class="font-medium">{{ ingredient.get_category_display }}</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Common Units:</span>
                            <span class="font-medium">{{ ingredient.common_units }}</span>
                        </div>
                        {% if ingredient.typical_expiry_days %}
                        <div class="flex justify-between">
                            <span class="text-gray-600">Typical Expiry:</span>
                            <span class="font-medium">{{ ingredient.typical_expiry_days }} days</span>
                        </div>
                        {% endif %}
                        <div class="flex justify-between">
                            <span class="text-gray-600">Created:</span>
                            <span class="font-medium">{{ ingredient.created_at|date:"M d, Y" }}</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Last Updated:</span>
                            <span class="font-medium">{{ ingredient.updated_at|date:"M d, Y" }}</span>
                        </div>
                    </div>
                </div>

                <!-- Actions -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4">Quick Actions</h3>
                    <div class="space-y-3">
                        <a href="{% url 'add_pantry_item' %}?ingredient={{ ingredient.id }}" 
                           class="w-full bg-green-500 hover:bg-green-600 text-white py-3 px-4 rounded-lg text-center block transition-colors duration-200 font-medium">
                            Add to Pantry
                        </a>
                        <a href="{% url 'core:edit_ingredient' ingredient.id %}" 
                           class="w-full bg-blue-500 hover:bg-blue-600 text-white py-3 px-4 rounded-lg text-center block transition-colors duration-200 font-medium">
                            Edit Ingredient
                        </a>
                        <a href="{% url 'ingredient_list' %}" 
                           class="w-full bg-gray-500 hover:bg-gray-600 text-white py-3 px-4 rounded-lg text-center block transition-colors duration-200 font-medium">
                            Back to List
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}